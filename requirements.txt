# Unsloth (install separately — see instructions below)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# pip install --no-deps xformers trl peft accelerate bitsandbytes

torch>=2.1.0
transformers>=4.41.0
accelerate>=0.31.0
peft>=0.11.0
bitsandbytes>=0.42.0
datasets>=2.14.0
trl>=0.8.0
gradio>=4.0.0
sentencepiece
huggingface-hub>=0.20.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# Evaluation
# easse: install from source
# git clone https://github.com/feralvam/easse.git && cd easse && pip install -e .
sacrebleu>=2.3.0
bert-score>=0.3.13
textstat>=0.7.3



